/build/
# `bear -- make` output: absolute paths, so it belongs to one checkout only.
# compile_flags.txt is the committed, portable equivalent.
/compile_commands.json
*.o
*.d
*.tmp
tests/artifacts/

# Rootfs images the test harness looks for (tests/lib.sh), fetched by CI.
/tests/.cache/
